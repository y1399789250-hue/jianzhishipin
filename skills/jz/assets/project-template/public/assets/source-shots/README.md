Save cropped evidence screenshots for source-backed `$jz` videos in this folder.

Each image used in the video must also be listed in `src/source-manifest.json` and inserted as a layer in `src/script.json`.
